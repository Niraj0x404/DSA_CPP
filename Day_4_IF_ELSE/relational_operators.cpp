#include <iostream>
using namespace std;
int main()
{
    int x = 4;
    int y = 5;
    cout << (x != y) << endl; // not equal to
    bool a = (x != y);
    cout << a << endl;
    cout << (x > y) << endl;
    cout << (x < y) << endl;
    cout << (x < y) << endl;
    cout << (x >= y) << endl;
}
